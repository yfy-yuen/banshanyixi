# 半山·一席 绑定 yuen-cloud 全流程（SOP）

> 适用场景：你决定**放弃 cloud1（账号 B）**，改用 **yuen-cloud（账号 A，AI 工具能直接管理）** 作为后端。
> 好处：之后建表 / 灌数据 / 部署云函数 / 排错，我都能直接用工具帮你做，不用再卡在另一个账号。
> 代价：现有 `restaurant-mini-mp` 跑通的是 **cloud1 文档库**代码，必须改写成 **PostgreSQL** 后端；且需你新注册一个微信小程序。

---

## 〇、角色分工（先看清谁干什么）

| 阶段 | 谁来做 | 在哪做 | 关键动作 |
|---|---|---|---|
| 一、微信侧 | **你** | mp.weixin.qq.com | 注册/确认小程序，拿到 AppID |
| 二、控制台 | **你** | console.cloud.tencent.com | 把 AppID 绑到 yuen-cloud |
| 三、云后端 | **我（AI）** | 工具直接操作 yuen-cloud | 改写后端为 PG、部署云函数、验表灌数 |
| 四、编译测试 | **你** | 微信开发者工具 | 导入工程、编译、模拟器/真机验证 |
| 五、老板身份 | **你/我** | 开门页 / staff 表 | 解锁商家权限 |

> ⚠️ **第 1~4 步必须由你做**（注册/绑定微信小程序是微信公众平台操作，我访问不到）。第 5~6 步我来做，**不依赖你先注册完**——云函数跑在 yuen-cloud 的 PG 上，和最终用哪个小程序调用无关，可提前开工。

---

## 阶段一：微信侧（你做）

### Step 1 — 确认「主体一致性」（2 分钟，先决条件）
**在哪**：[mp.weixin.qq.com](https://mp.weixin.qq.com) 用你的微信登录

**为什么重要**：微信小程序账号 和 腾讯云账号 A（`100045779667`）**必须是同一主体**（同一身份证 / 同一营业执照），绑定才顺畅。否则会出现跨账号，又回到 cloud1 那种"AI 碰不到"的坑。

**操作**：
1. 登录后看首页「你名下是否已有小程序/公众号」。
2. 若有 → 检查它的**主体**是否 = 腾讯云账号 A 的主体。
   - 相同 → 直接用它，跳到 Step 3 拿 AppID。
   - 不同 → 不能绑 yuen-cloud，必须走 Step 2 新注册。
3. 若没有 → 走 Step 2。

**完成标志**：你明确知道"有一个主体=账号 A 的小程序"或"需要新注册一个"。

### Step 2 — 注册新小程序（仅当 Step 1 需要）
**在哪**：mp.weixin.qq.com → 右上「立即注册」→ 选「小程序」

**操作**：
1. 用**和腾讯云账号 A 相同主体**的资质注册（个人：实名身份证；企业：营业执照）。
2. 完成邮箱激活、信息登记、主体认证。
3. 注册成功后进入该小程序后台。

**注意**：
- 个人主体小程序**不支持**微信支付等部分类目，私人小店够用但要心里有数。
- 类目可选「餐饮-点餐」或先用「工具」，后续再调。

**完成标志**：拿到新小程序的 **AppID**（格式 `wx` 开头，在「开发 → 开发管理 → 开发设置」里）。

### Step 3 — 把 AppID 发给我
把 Step 2（或 Step 1 复用）拿到的 AppID 发给我。**不要发密钥（AppSecret）**，只需要 AppID。

> 这一步发完，你就可以先歇着——第 5~6 步我并行开工，不用等你做 Step 4。

---

## 阶段二：CloudBase 控制台（你做）

### Step 4 — 把 AppID 绑定到 yuen-cloud
**在哪**：[console.cloud.tencent.com](https://console.cloud.tencent.com) → 云开发 CloudBase → 环境 `yuen-cloud` → **环境设置**（或「登录授权 / 微信小程序」页）

**操作**：
1. 确认当前登录的腾讯云账号 = 账号 A（`100045779667`）。
2. 找到「微信小程序」绑定入口 → 填入 Step 3 的 AppID → 确认绑定。
3. 同时在「登录授权」页**开启「匿名登录」**（老板身份/角色判定依赖它；不开则角色永远降级为顾客）。

**完成标志**：yuen-cloud 的 `WxAppId` 不再是空值（可在环境信息里看到已绑 AppID）。

---

## 阶段三：云后端（我来做，用工具）

> 以下我直接用 CloudBase 工具操作 **yuen-cloud**（已验证可访问），你无需操作，只在我卡住时补信息。

### Step 5 — 改写后端为 PostgreSQL + 部署
**我做的内容**：
1. 把 `restaurant-mini-mp` 的 3 个云函数（`dataApi` / `submitOrder` / `initData`）从「文档库 `cloud.database()`」改写成「**PostgreSQL 直连**」（`pg` 包 + 连接串，绕开 Node 端 `rdb()` 不可用的坑）。
2. 前端调用方式**不变**：仍走 `utils/api.js` 的 `callApi(action)` → `dataApi` 云函数，只是云函数内部换成 PG 查询。
3. 用工具把 3 个云函数**部署到 yuen-cloud**（云端安装依赖）。
4. 验证 yuen-cloud 的 8 张表（`dishes/orders/rooms/bookings/staff/staff_requests/payment_qrcodes/user_roles`）结构，必要时补字段。
5. 配置 PG **RLS 行级安全策略**（让匿名/已登录用户能读菜单、店主能改）。
6. 调一次 `initData` 灌/校验示例数据（5 道菜 + 5 包厢）。

**完成标志**：云函数部署成功、工具侧能 `queryPgDatabase` 读到菜单数据。

### Step 6 — 改工程配置
**我做的内容**：
1. `project.config.json` / `app.js` 的 **AppID** 改成你的新小程序 AppID（替换原来的 `wx4beadc7d17bba483`）。
2. `utils/config.js` 的 **ENV** 改回 `yuen-cloud-d9g0mo1bv654d2ff6`。
3. 移除 cloud1 文档库相关残留引用。

**完成标志**：工程配置指向 yuen-cloud + 新 AppID。

---

## 阶段四：编译与测试（你做）

### Step 7 — 微信开发者工具导入并编译
**在哪**：微信开发者工具

**操作**：
1. 导入项目：目录选 `C:\restaurant-mini-mp`，AppID 填你的新小程序 AppID（**不要用测试号**）。
2. 编译 → 模拟器打开「开门页」。
3. 点「推门入席」→ 进包厢页，**确认菜单能加载出 5 道菜**。
4. 真机预览（右上「真机调试」）验证一次。

**完成标志**：菜单正常显示、下单流程可走通（无 `-504002` / `Env Not Exists` 等报错）。

**若报错**：把控制台报错发我，我回云侧查（多是 RLS 策略 / 云函数部署 / 连接串问题）。

---

## 阶段五：老板身份（你/我）

### Step 8 — 解锁商家权限
两种方式二选一：
- **A. 本地解锁（最快，不依赖云端）**：开门页底部「商家入口」→ 输 `Boss8888` → 本机直接变老板（已在代码里接好）。
- **B. 云端正式途径**：在 `staff` 表插 `{openid: 你的uid, role:'manager'}`（或 `config.js` 的 `OWNER_UID` 填你的匿名 uid）。

**完成标志**：开门页出现「商家」tab，能看到商家管理内容。

---

## 关键提醒（务必看）

1. **yuen-cloud 是体验版，QPS 500** —— 私人小店够用，量大要升级。
2. **体验版 2027-02-15 到期** —— 之前记得续费/迁移。
3. **PG 权限用 RLS，不是 NoSQL 集合规则** —— 我会在 Step 5 配好，你不用管。
4. **隐私**：`OWNER_UID`、老板解锁码 `Boss8888` 等敏感值只留在代码里，我不会在对话回显，也不要写进记忆/文档明文（本文已用占位，具体值见 `utils/config.js`）。
5. **不要混用**：原 cloud1 小程序 `wx4beadc7d17bba483` 与此流程无关，绑定完成后可弃用。

---

## 失败排查速查

| 现象 | 可能原因 | 处理 |
|---|---|---|
| 导入时报「AppID 不对 / 不在该账号」 | Step 4 没绑 or 绑错账号 | 回 Step 4 确认绑定在账号 A |
| 菜单加载失败 / `-504002` | 云函数没部署 or RLS 挡了读 | 发我报错，我查 yuen-cloud |
| `Env Not Exists` | ENV 写错 | 确认 `config.js` 的 ENV = `yuen-cloud-d9g0mo1bv654d2ff6` |
| 看不到「商家」tab | 匿名登录没开 / 老板未解锁 | Step 4 开匿名登录 + Step 8 解锁 |
| 编译慢 | 云函数冷启动 | 首次 2~8 秒，正常 |
