# 半山·一席 点菜小程序 · 零基础运行指南

你不用懂代码，照着下面**点鼠标**就能在电脑上看到点餐界面，并让它在手机上真机运行。
（依赖我已经帮你装好了，你从「二」开始即可。）

> 本小程序使用**微信云开发**（环境 `cloud1-d9gs6p6t18e19cff9`，文档数据库），所有数据读写都走云函数。这和早期的「独立腾讯云 + PostgreSQL」方案**不是一回事**，请一律以本指南为准。

---

## 一、第一次：安装微信开发者工具（只需做一次）

1. 浏览器打开：https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html
2. 下载「**稳定版 Stable Build**」→ 选 **Windows 64 位**
3. 双击安装，一路「下一步」
4. 第一次打开时，用**你注册小程序的那个微信**扫码登录

---

## 二、导入项目（只需做一次）

1. 微信开发者工具里，点「**导入项目**」（或左上角「+」）
2. **项目目录**选这个文件夹：
   `C:\restaurant-mini-mp`
3. **AppID** 一栏：会自动显示 `wx4beadc7d17bba483`（就是你的小程序 AppID；若是空白就手动填这个）
4. **后端服务**：选「**微信云开发**」
   ⚠️ 很重要：本项目用的是微信云开发（`wx.cloud.callFunction`），**必须选「微信云开发」**，千万别选「不使用云开发」，否则菜单/下单全部连不上。
5. 点「**导入**」

---

## 三、构建 npm（只需做一次）

1. 顶部菜单点「**工具**」→「**构建 npm**」
2. 等右下角弹出「**构建成功**」
3. 若提示「NPM packages not found / 未找到 node_modules」：多半导入的是旧的中文路径目录，请「项目 → 退出当前项目」，重新「导入项目」选 `C:\restaurant-mini-mp`（纯英文路径）再做本步
4. 若提示 npm 报错：点右侧「**详情**」→「**本地设置**」，勾选「**使用 npm 模块**」，再重做本步

---

## 四、云端一次性设置（只需做一次，但**必须做**，否则白屏/报错）

下面的操作都在微信开发者工具里完成。导入项目后，点顶部「**云开发**」按钮打开云开发控制台。

### 4.1 开启登录方式
- 云开发控制台 → **设置** → **登录方式**
- 开启「**匿名登录**」（顾客进店识别身份、显示「商家」tab 需要）
- 开启「**账号密码登录**」（商家/店员登录后台需要）

### 4.2 创建商家账号（可选，但看后台需要）
- 云开发控制台 → **用户管理** → **新建用户**
- 建两个账号：
  - 用户名 `boss`，密码 `Boss8888`（老板/店长）
  - 用户名 `clerk`，密码 `Clerk1234`（店员）
- 这两个账号的「身份标识(uid)」稍后要在 `staff` 集合里登记角色（见 4.5）。

### 4.3 创建数据库集合
- 云开发控制台 → **数据库** → **集合列表** → **新建集合**
- 依次新建以下 7 个集合（名字要一模一样）：
  `dishes` `orders` `rooms` `bookings` `staff` `staff_requests` `payment_qrcodes`
- 集合权限保持默认即可（所有读写都经云函数管理员身份，不受集合权限限制）。

### 4.4 部署云函数（关键）
在左侧文件树里，对以下 3 个云函数**分别**右键 →「**上传并部署：云端安装依赖**」：
- `cloudfunctions/dataApi`（所有读写的中转）
- `cloudfunctions/submitOrder`（提交订单）
- `cloudfunctions/initData`（灌示例数据，部署一次即可）

> 部署时务必选「云端安装依赖」，否则云函数找不到 `wx-server-sdk` 会报错。

### 4.5 灌入示例数据
- 云开发控制台 → **云函数** → 点 `initData` → 右键「**测试**」（或「云端测试」）
- 直接点「运行」，返回类似 `{ "data": { "ok": true, "dishes": 5, "rooms": 5 } }` 即成功
- 这一步会在 `dishes` 写入 5 道示例菜、`rooms` 写入 5 个包厢，菜单才有内容。

### 4.6 老板/店长身份怎么生效
「商家」tab 只对 `manager` 角色显示。两种办法二选一：
- **办法 A（最简单，用代码常量）**：在模拟器 Console 里看日志 `[role] 当前匿名 uid = xxxx`，把 `utils/config.js` 里的 `OWNER_UID` 改成这个值（需你授权我改，且匿名 uid 换设备/清缓存会变，仅供本地调试）。
- **办法 B（正式，写库）**：在 `staff` 集合插入一条记录：
  `{ "openid": "<你当前的匿名 uid 或密码账号 uid>", "name": "老板", "role": "manager" }`
  商家账号（boss/clerk）登录后，系统会按该账号的 uid 去 `staff` 查角色，所以**密码账号也要在 `staff` 里登记对应 openid 和 role**（`manager` 或 `clerk`），否则登录后提示「未配置角色」。

---

## 五、运行 / 预览（以后每次都这样）

1. 点工具栏「**编译**」（或 Ctrl+B）→ 中间模拟器出现点餐界面
2. 点一个包厢（如「谷山玥」）→ 菜单加载出来（首次云函数冷启动可能慢几秒，属正常）
3. 加几道菜 → 点右下角「**去结算**」→ 提交
4. 点底部「**我的订单**」查看刚下的单
5. 点底部「**商家**」→ 用 `boss` / `Boss8888` 登录，看后台（需已完成 4.2/4.6）
6. 想用手机试：点「**预览**」→ 微信扫码

---

## 六、遇到报错这样自查

| 现象 | 怎么办 |
|------|--------|
| 白屏 / 报 `useAdapters` 或 `require` 相关错 | 没构建 npm，回到「三」重做 |
| 菜单/下单报 `Env Not Exists` | 看 `utils/config.js` 的 `ENV` 是不是 `cloud1-d9gs6p6t18e19cff9`；确认导入时选了「微信云开发」 |
| 报 `函数不存在 / 找不到 dataApi` | 回到「四·4.4」把 3 个云函数都「上传并部署：云端安装依赖」 |
| 菜单一片空白，Console 报集合不存在 | 回到「四·4.3」建集合，并执行「四·4.5」initData 灌数据 |
| 商家登录提示「未配置角色」 | 回到「四·4.6」，在 `staff` 集合按账号 uid 登记 role |
| 手机扫码报域名相关错 | 开发期在「详情 → 本地设置」勾选「不校验合法域名」；`wx.cloud` 走微信域名，正式发布一般也无需额外配置 request 合法域名 |

---

## 七、发布上线前才需要做（现在不用管）

- 确认云函数已全部「上传并部署：云端安装依赖」并验证真机可用。
- 把 `utils/config.js` 里硬编码的 `OWNER_UID` 改为从环境变量/配置读取（避免随代码扩散），并确认不出现在任何日志/界面。
- 若开启「增强编译」导致白屏，保持 `project.config.json` 的 `"enhance": false`（已设）。
