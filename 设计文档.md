# 半山·一席 微信小程序 · 详细设计文档

> 版本：v1.1 ｜ 日期：2026-08-16 ｜ 状态：RBAC 已确认，待落地
> 适用范围：客人端（大门→包厢→入席查看+点单）+ 员工端（预定/管理，微信身份 RBAC）
> 视觉基线：水墨风（青灰宣纸 / 米白卡 / 淡墨 / 朱砂），复用既有 `app.wxss` 设计 token

---

## 1. 项目背景与目标

「半山·一席」为私人菜馆（山坡22号），采用**预售制**：客人线下提前订位，到店后按所订包厢入席。
小程序要解决三件事：

1. **入席查看**：客人到店 → 大门 → 选自己包厢 → 看环境、看预定单、看菜单。
2. **预售预定**：员工按日历为客人占位（包厢 + 日期 + 餐段 + 用餐/棋牌 + 菜品）。
3. **到店点单**：客人在包厢内通过共用菜单加菜下单（既有流程保留）。

设计原则：客人端极简、沉浸式；管理端按微信身份分级；四厢视觉统一，内容按包厢区分。

---

## 2. 信息架构（IA）总览

```
半山·一席
├─ 宫廷大门（落地页，仅首次）        点击进入
└─ 底部 TabBar（按角色动态显示）
   ├─ 包厢（所有人可见）
   │  ├─ 包厢列表（4 卡）
   │  └─ 包厢内页（三分页）
   │     ├─ 环境   本厢照 + 餐厅照（各厢不同）
   │     ├─ 订单   本厢预定单（左图右名，各厢不同）★来自 bookings
   │     └─ 菜单 → 共用菜单页（既有点餐流程，加菜下单）
   ├─ 预定（仅 clerk / manager）      员工预售后台
   │  └─ 日历 → 选包厢 → 选餐段 → 选类型 → 录菜品 → 生成预定单
   └─ 商家（仅 clerk / manager）      管理入口（原 merchant 页，角色门控）
      └─ 订单 / 菜品 / 报表 / 收款码 / 员工管理(店长)
```

**关键决策**
- 不做 4 套包厢页，做 **1 个包厢内页模板 + 按 `room_id` 加载数据**。
- 「预定单（订单 tab）」由员工预售录入（`bookings` 表），客人端只查看。
- 「菜单」四厢共用同一页面；其购物车下单为既有"到店加菜"流程（生成 `orders`）。
- 权限统一为**微信身份 RBAC**（见 §2.1），替代原 merchant 页的密码登录。

---

## 2.1 角色与权限（RBAC）

### 身份获取
1. 小程序启动即 `wx.login()` → 调云函数 `getOpenid` → 返回 `openid`（服务端换，不暴露 AppSecret）。
2. 拿 openid 查 `staff` 表：不在→顾客；`clerk`→店员；`manager`→店长。
3. 角色写入 `app.globalData.role`，驱动 TabBar 与页面显隐。

> openid 是微信给每个用户在本小程序的匿名编号，系统自动获取，**员工/顾客都不用手动填**。

### 三级权限矩阵
| 能力 | 顾客 | 店员 clerk | 店长 manager |
|------|------|-----------|--------------|
| 看包厢 / 环境 / 预定单 / 菜单 | ✓ | ✓ | ✓ |
| 菜单加菜下单 | ✓ | ✓ | ✓ |
| 预定管理（排席/录菜） | ✗ | ✓ | ✓ |
| 菜品 / 订单 / 报表 / 收款码 | ✗ | ✓ | ✓ |
| 员工管理（审批/设角色/移除） | ✗ | ✗ | ✓ |

### 数据表
```sql
-- 员工白名单（微信身份）
staff (
  openid      text primary key,
  name        text,
  role        text,               -- 'clerk' | 'manager'
  invited_by  text,               -- 审批人 openid
  created_at  timestamptz default now()
);

-- 员工入职待审
staff_requests (
  openid      text primary key,
  name        text,
  status      text default 'pending',  -- pending | approved | rejected
  requested_at timestamptz default now()
);
```

### 入职流程（申请 + 店长审批）
1. 员工打开小程序 → 在「商家」页点「申请成为员工」→ 填姓名 → 写 `staff_requests(status='pending')`。
2. 店长在「商家 → 员工管理」看到申请 → 点批准，选 `clerk` / `manager` → 写 `staff` 并置请求 `approved`。
3. 完成。全程不出现 openid。
4. **初始店长**：你（老板）在 CloudBase 控制台插一行 `staff(openid, name, role='manager')` 一次即可（你的 openid 可在首次打开小程序后的 Console 或调试页取得）。

---

## 3. 导航与路由

| 页面 | 路由类型 | 角色可见 | 说明 |
|------|----------|----------|------|
| 宫廷大门 | 启动页（`pages/gate/gate`） | 全部 | 首屏；首次显示，本地记 `gateShown` |
| 包厢列表 | TabBar（`pages/rooms/rooms`） | 全部 | 4 卡 |
| 包厢内页 | 普通页（`pages/room/room?room=厢X`） | 全部 | `navigateTo`，参数传包厢 |
| 共用菜单 | 普通页（`pages/menu/menu`） | 全部 | 既有点餐流程 |
| 预定后台 | TabBar（`pages/book/book`） | clerk/manager | 日历 + 录入 |
| 商家管理 | TabBar（`pages/merchant/merchant`） | clerk/manager | 原 merchant 改造为角色门控 |

> **TabBar 按角色动态显示**：采用自定义 TabBar（`"tabBar": { "custom": true }`）。
> - 顾客：包厢 / 商家（无「预定」）
> - 员工：包厢 / 预定 / 商家
>
> 说明：原生 TabBar 的 item 无法运行时增删，故用自定义 TabBar 组件读取 `globalData.role` 渲染对应项；各 Tab 页 `onShow` 调用 `this.getTabBar().setData({selected, role})` 高亮。

---

## 4. 页面设计规范

### 4.1 宫廷大门（落地页）
全屏水墨山水背景（复用 `rooms.wxss` 的 SVG 山水 data-url）＋ 居中匾额「半山·一席」＋ 朱砂印章 ＋ 「推门入席」按钮。
- 首次启动显示；点击 → `switchTab` 到包厢列表，并写 `wx.setStorageSync('gateShown', true)`。
- 非首次：直接 `switchTab` 到包厢，不显示大门。

### 4.2 包厢列表（rooms）
纵向 4 张卡（2×2 网格）：环境缩略图（或厢名书法）＋ 厢名 ＋ 「入席 →」。
- 点击卡片 → `navigateTo` 包厢内页，带 `room=厢X`。
- 注：现有 `ROOMS` 配置为 `{1:谷山玥, 2:满仓, 3:枕山, 5:云起}`（无 4 号），包厢内页数据以 `rooms` 表为准。

### 4.3 包厢内页（room）— 三分页
顶部厢名＋分段控件（环境 / 订单 / 菜单），内容区随分段切换。

**环境（各厢不同）**：横向滑动图集（本厢照 + 餐厅公共照），每图一行说明；空态「本厢环境照整理中」。

**订单（各厢不同）★来自 bookings**：顶部信息条「日期 · 餐段(午/晚) · 类型(用餐/棋牌)」；菜品列表每行**左图(菜)｜右 名称 + 款式 + 数量**；默认显示**今天该厢那一场**，无则显示最近一场；空态「本厢今日尚未排定席次」。

**菜单**：点击「菜单」分段 → `navigateTo` 共用 `menu` 页（既有购物车下单流程，四厢同一份）。

### 4.4 共用菜单（menu，既有）
分类展示（热菜/凉菜/饮品/主食/其他），每项：图 + 名称 + 价 + 说明；购物车加菜 → 提交生成 `orders`（既有流程）。本期保留。

### 4.5 预定后台（book）— 仅员工
日历（月视图）＋ 选中日期的「包厢 × 餐段」矩阵。
- 点「空闲」格（包厢＋餐段）→ 弹层：选类型（用餐/棋牌）→ 选菜品（多选，取自 `dishes`）→ 填客人姓名/电话/备注 → 保存写入 `bookings`。
- 冲突规则：`UNIQUE(room_id, date, slot)`；棋牌占该餐段；已订格置灰不可再点。

### 4.6 商家管理（merchant）— 仅员工
原 merchant 页改造为**角色门控**管理入口（移除原密码登录，改用微信身份）：
- 店员/店长：订单、菜品、报表、收款码。
- 店长额外：员工管理（申请列表 + 审批 + 设角色 + 移除）、以及「申请成为员工」入口（供新人在此发起）。

---

## 5. 视觉设计规范（水墨系统）
复用既有 token（`app.wxss`）：宣纸底 `#e2ded6`、米白卡 `#f4f0e8`、淡墨线 `#c9c4ba`、淡墨 `#6a655d`、主字 `#3d3d3d`、灰墨 `#8a857d`、朱砂 `#a04538`、古金 `#a68b62`。衬线标题、8px 基线、`.card`/`.btn`/`.divider`/`.seal` 组件；动效仅 `transform`/`opacity` 0.2–0.3s。

---

## 6. 数据模型（CloudBase PostgreSQL）

```sql
-- 包厢（各厢环境不同）
rooms (
  id text primary key, name text, env_photos text[], restaurant_photos text[], sort int, status int default 1
);

-- 预定单（员工预售录入；棋牌占餐段）★room 内页「订单」tab 数据源
bookings (
  id serial primary key,
  room_id text references rooms(id),
  date date,
  slot text,                 -- 'lunch' | 'dinner'
  type text,                 -- 'meal' | 'game'
  dishes jsonb,              -- [{dish_id,name,image,qty,note}]
  guest_name text, guest_phone text, note text,
  created_at timestamptz default now(),
  unique(room_id, date, slot)
);

-- 到店点单（既有流程，menu 购物车提交）-- 结构沿用既有 orders
orders (
  id serial primary key, room_no text, room_name text, people int,
  items jsonb, total numeric(10,2), status text default 'unpaid',
  created_at timestamptz default now()
);

-- 共用菜单（四厢同一份）
dishes (
  id serial primary key, name text, image text, price numeric(10,2),
  category text, desc text, sort int
);

-- 员工白名单（微信身份 RBAC）
staff (
  openid text primary key, name text, role text, invited_by text, created_at timestamptz default now()
);
-- 员工入职待审
staff_requests (
  openid text primary key, name text, status text default 'pending', requested_at timestamptz default now()
);
```

---

## 7. 关键交互与状态
| 场景 | 处理 |
|------|------|
| 大门首启 / 非首启 | 显示写 `gateShown` 跳包厢 / 跳过 |
| 角色未知（openid 查询失败） | 降级为顾客视图，不阻断使用 |
| 包厢无环境图 | 环境分页空态 |
| 包厢今日无预定 | 订单分页空态，显示最近一场 |
| 预定冲突 | 该格置灰，保存时后端 `unique` 兜底报错 |
| 非员工进管理 Tab | 自定义 TabBar 不渲染该 Tab；若直达则页内提示无权限 |

---

## 8. 无障碍（WCAG 2.2 AA）
正文对比度达标；按钮≥44×44px；状态不只靠颜色（已订格加文字）；分段控件文字+下划线；图片加替代文案。

---

## 9. 实施里程碑
| 里程碑 | 内容 | 复杂度 |
|--------|------|--------|
| M1 后端脚手架 | `getOpenid` 云函数 + `staff`/`staff_requests`/`bookings` 建表 + `callFunction` 封装 | 中 |
| M2 角色骨架 | app.js 取 openid+角色、custom TabBar 按角色渲染、merchant 改角色门控 | 中 |
| M3 顾客端 | 大门 + 包厢列表 + 包厢内页三分页 + 菜单复用 | 中低 |
| M4 预定后台 | 日历 + 餐段矩阵 + 录入 + 冲突 | 中 |
| M5 员工入职 | 申请入口 + 店长审批 + 角色/移除 | 中 |
| M6 打磨 | 空态/动效/真机校验 | 低 |

---

## 10. 待确认 / 后续项（已全部确认）
- [x] **权限合并方式**：已统一为微信身份 RBAC，移除 merchant 密码登录；custom TabBar 按角色渲染（顾客仅见「包厢」，店员/店长见三栏）。
- [x] **包厢命名/数量**：沿用现有 `ROOMS` = 谷山玥(1)/满仓(2)/枕山(3)/云起(5)，数字与名称不变，无 4 号。
- [x] **订单页分两类**：包厢内页「订单」与商家「订单」均内嵌分段——
  - 【订餐菜品】= 预售 `bookings`（按包厢/日期/餐段）；
  - 【现场下单菜品】= 到店 `orders`（menu 购物车提交）。
- [x] **棋牌分午/晚**：标签为 `午间棋牌`(lunch+game) / `晚间棋牌`(dinner+game)；用餐为 `午餐订餐` / `晚餐订餐`。
- [x] **预定后台取消/改期**：已排格显示「改期 / 取消」；改期弹窗支持改日期+改餐段（含冲突校验），取消即删除 `bookings` 记录。

---

## 11. 实施状态（截至 2026-08-16）
| 里程碑 | 状态 |
|--------|------|
| M1 后端脚手架（schema + 角色初始化） | 已完成 |
| M2 角色骨架 + custom TabBar | 已完成（app.js 匿名登录取 uid→查 staff 定角色；custom-tab-bar 按角色渲染；merchant 改门控） |
| M3 顾客端（大门/列表/包厢内页三分页/菜单复用） | 已完成 |
| M4 预定后台（日历+矩阵+录入+冲突） | 已完成 |
| M5 员工入职（申请+店长审批+移除） | 已完成（merchant.js/wxml/wxss 改造、wxss 补缺失类） |
| M6 打磨（空态/动效/真机校验） | 待真机编译验证 |

**上线前必做**：
1. 真机 / 微信开发者工具编译验证（本次改动未跑过）。
2. 初始店长：按 `db/schema.sql` 注释，把自己匿名登录的 uid 写进 `staff` 表（`role='manager'`）。顾客页「员工专区」会显示「我的身份ID」，复制给本人即可。
3. 在 CloudBase 控制台执行 `db/schema.sql` 建表（`rooms/bookings/orders/dishes/staff/staff_requests`）。
