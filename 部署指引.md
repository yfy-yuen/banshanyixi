# 半山·一席 小程序 · 云函数部署指引（dataApi 等）

> 本文是「按表格逐个落地」任务队列的第一项（#190 部署 dataApi）+ 第二项（#191 订阅推送激活）的配套交付物。
> 代码侧已全部就绪并校验通过，你只需要在**微信开发者工具**里点几下即可上线。

---

## 一、已就绪核验（AI 已完成，可放心部署）

| 项 | 结果 |
|---|---|
| 云函数语法 `node --check` | ✅ dataApi / submitOrder / initData / backfillTags 全部通过 |
| 前端 action 与云函数匹配 | ✅ 前端调用的 **38 个** action 全部在 dataApi 中有对应实现 |
| 依赖 `wx-server-sdk ~2.6.3` | ✅ 4 个云函数 package.json 均声明 |
| 云环境一致 | ✅ AppID `wx4beadc7d17bba483`，ENV `cloud1-d9gs6p6t18e19cff9` |
| 订阅模板 ID | ✅ 前端 `config.js` 已填 `RESERVE_TPL_ID`；dataApi 已加兜底常量，部署即生效 |
| 顾客端授权 | ✅ `pages/reserve/reserve.js` 在提交订位时 `wx.requestSubscribeMessage` 已接好 |

> ⚠️ 历史踩坑提醒：早期总结误以为云函数用 `getMerchantOrders` / `loadBookings` 两个 action 名，实际代码用的是 `merchantOrders` 和 `bookings`，**与前端一致、正确**，无需改动。

---

## 二、前置：在 cloud1 控制台建集合（只需做一次）

微信云开发控制台 → 数据库 → 集合管理，新建以下 **7 个**集合（名称务必完全一致，区分大小写）：

| 集合名 | 用途 | 是否需手动建 |
|---|---|---|
| `dishes` | 菜品 | ✅ 手动 |
| `orders` | 现场订单（submitOrder 写入） | ✅ 手动 |
| `rooms` | 包厢（环境照/介绍） | ✅ 手动 |
| `bookings` | 预订排席（订位确认后生成） | ✅ 手动 |
| `staff` | 员工/老板（OPENID 角色锚定） | ✅ 手动 |
| `staff_requests` | 员工加入申请 | ✅ 手动 |
| `payment_qrcodes` | 收款码 | ✅ 手动 |
| `sessions` | 订位场次 | 🔁 首次调用自动建（ensureCol） |
| `reservations` | 订位申请 | 🔁 首次调用自动建 |
| `owner_boot` | 老板一次性采集 | 🔁 首次调用自动建 |

> 集合权限：默认选「仅创建者可读写」即可，所有读写都走云函数（管理员权限），前端不直接连集合。

---

## 三、部署云函数（微信开发者工具）

1. 用**微信开发者工具**打开本项目（`C:\restaurant-mini-mp\`，AppID 已是 `wx4beadc7d17bba483`）。
2. 在左侧文件树展开 **`cloudfunctions/`**，对以下**每个**文件夹分别右键 → **「上传并部署：云端安装依赖」**：
   - `dataApi`
   - `submitOrder`
   - `initData`
   - `backfillTags`（可选，仅用于一次性回填标签）
3. 部署时若提示「已存在同名云函数」→ 选 **「上传并部署：云端安装依赖」**（覆盖），不要用「新建并部署」。
4. 等待每个都显示「上传成功」。

> 关键：必须选 **云端安装依赖**，让云端自动 `npm install wx-server-sdk`；不要选「本地安装依赖」否则可能漏装。

---

## 四、#191 订阅推送激活（环境变量，可选但建议）

推送现在**不需要**在控制台配环境变量也能工作（dataApi 已内置兜底常量 `RESERVE_TPL_ID`）。

若你想用控制台环境变量覆盖（方便以后换模板不改代码），可这样做：
- 云开发控制台 → 云函数 → `dataApi` → **配置** → 环境变量 → 新增：
  - 键：`RESERVE_TPL_ID`
  - 值：`kQPofhWMCtqHYs_DobSxURk0wvQF6k9o0_Rc0O-_uDE`
- 保存后**重新部署一次** `dataApi`。

另外需确认（一次性，大概率已做）：
- 微信公众平台 → 订阅消息 → 我的模板里存在模板 **「宴席预定即将到时提醒」**，模板 ID 与上一致；
- 模板字段代号必须是 `date1 / number2 / thing3 / thing4`（dataApi 第 430-437 行按此填充；若代号不同，改那里即可）。

> 注意：订阅消息**必须真机预览**才能收到（开发者工具里收不到推送，属正常）。顾客在提交订位时点了「允许」后，店务确认才会下发通知。

---

## 五、初始化示例数据（只做一次）

部署完成后，在微信开发者工具里：
- 云开发 → 云函数 → `initData` → **测试**（右键也可）→ 运行一次。
- 返回 `{ "data": { "ok": true, "dishes": <数量>, "rooms": 5 } }` 即成功。
- 这会把 `initData/index.js` 里的真实菜单（约 200 道）与 5 个包厢写入 `dishes` / `rooms` 集合（幂等，可重复跑）。

> 之后菜品维护在「商家端 → 菜品管理」（老板专属），不再需要跑 initData。

---

## 六、上线验证清单

按序确认：
1. 真机预览小程序 → 开门页「推门入席」能进包厢列表（gate 已锁死，无需动）。
2. 顾客端：能加载菜单、能现场下单（submitOrder 落 `orders`）。
3. 老板身份：开门页长按印章 → 输 `Boss8888` → 注册为 manager（写入 `staff`）。
4. 商家端：订单 tab 能看到订单、「后厨配菜单 · 一键出单」能进 kitchen 页。
5. 订位：顾客提交订位申请 → 商家端「全部预订」出现 → 确认 → 自动生成 booking 并（真机）收到订阅推送。
6. 收尾：清台 / 售罄标记 / 收款码等写库动作不再报「云函数不存在」。

---

## 七、常见报错速查

| 现象 | 原因 | 处理 |
|---|---|---|
| 调用报错「云函数不存在 / 未找到 dataApi」 | 没部署或部署到别的环境 | 回到第三步重新「上传并部署：云端安装依赖」 |
| 调用报 `-504002` / 数据库权限 | 集合没建或环境不对 | 第二步建齐 7 个集合；确认 ENV=cloud1 |
| 菜单空白 | `dishes` 集合为空 | 第五步跑 initData |
| 订位确认后顾客收不到推送 | 开发者工具不收推送；或顾客没点允许 | 真机预览 + 顾客授权；检查模板 ID / 字段代号 |
| `openapi` 报错「无权限」 | 没开通订阅消息能力 / 模板 ID 错 | 公众平台确认模板存在且 ID 一致 |

---

## 八、下一步

本指引覆盖 #190（部署）+ #191（订阅激活）的「代码就绪 + 你点一下」部分。
后续队列（已建任务 #192–#207）按表格顺序继续：
🔴 #192 about 页真实门店信息 → 🟡 #194 预点必填前置、#193 电子收据、#198 采购清单、#196 时间口径、#195 自动分厢改厢 UI …… 等。
