# cloud1 云端部署清单（必须人工在控制台完成）

> 代码已全部就绪，但云环境在**你的小程序账号（账号 B）**下，我（AI 工具在另一账号）访问不到，以下 7 步需要你在微信开发者工具「云开发」控制台**手动操作**。每完成一项打勾 ✅。

环境：微信云开发 `cloud1-d9gs6p6t18e19cff9`（小程序 AppID `wx4beadc7d17bba483`）

---

### ✅ 1. 开启登录方式
- 云开发控制台 → **设置** → **登录方式**
- 开启「**匿名登录**」
- 开启「**账号密码登录**」

### ✅ 2. 创建商家账号（看后台才需要）
- 云开发控制台 → **用户管理** → **新建用户**
- `boss` / `Boss8888`（店长）
- `clerk` / `Clerk1234`（店员）
- 记下这两个账号的 **uid**（身份标识），下一步登记角色要用

### ✅ 3. 创建 7 个集合
- 云开发控制台 → **数据库** → **集合列表** → **新建集合**
- 依次建（名字一字不差）：
  - [ ] `dishes`
  - [ ] `orders`
  - [ ] `rooms`
  - [ ] `bookings`
  - [ ] `staff`
  - [ ] `staff_requests`
  - [ ] `payment_qrcodes`
- 权限保持默认（读写都走云函数管理员身份）

### ✅ 4. 部署 3 个云函数
左侧文件树，对以下每个**右键 →「上传并部署：云端安装依赖」**：
- [ ] `cloudfunctions/dataApi`
- [ ] `cloudfunctions/submitOrder`
- [ ] `cloudfunctions/initData`
> 务必选「云端安装依赖」，否则云函数找不到 `wx-server-sdk` 会 145 报错。

### ✅ 5. 灌示例数据
- 云开发控制台 → **云函数** → 点 `initData` → 右键「**测试**」
- 直接「运行」，返回 `{ "data": { "ok": true, "dishes": 5, "rooms": 5 } }` 即成功
- （重复运行幂等，不会叠加）

### ✅ 6. 老板/店长身份
「商家」tab 只对 `manager` 显示，二选一：
- **A. 代码常量（本地调试）**：模拟器 Console 看 `[role] 当前匿名 uid = xxxx`，把 `utils/config.js` 的 `OWNER_UID` 改成它（需授权；匿名 uid 换设备/清缓存会变）
- **B. 写库（正式）**：在 `staff` 集合插入
  ```json
  { "openid": "<当前匿名 uid 或密码账号 uid>", "name": "老板", "role": "manager" }
  ```

### ✅ 7. 商家账号 ↔ staff 角色对应
密码账号（boss/clerk）登录后，系统按**该账号的 uid** 去 `staff` 查 `role`。所以在 `staff` 集合里也要为它们登记：
- [ ] `{ "openid": "<boss 账号 uid>", "name": "老板", "role": "manager" }`
- [ ] `{ "openid": "<clerk 账号 uid>", "name": "店员", "role": "clerk" }`
> 没登记会登录后提示「未配置角色」。

---

## 完成判据
编译 → 选包厢 → 菜单出 5 道菜 → 加菜结算 → 「我的订单」可见 → （可选）用 `boss/Boss8888` 登录「商家」看到后台。

## 常见问题
| 报错 | 原因 / 处理 |
|------|------|
| `Env Not Exists` | `utils/config.js` 的 `ENV` 不是 `cloud1-d9gs6p6t18e19cff9`，或导入时没选「微信云开发」 |
| `函数不存在 / 找不到 dataApi` | 第 4 步漏部署，或没选「云端安装依赖」 |
| 菜单空白 + 集合不存在 | 第 3/5 步没做 |
| 商家登录「未配置角色」 | 第 7 步 `staff` 没按账号 uid 登记 |
| 冷启动慢 | 云函数首次/久未调用拉起容器，2–8 秒正常 |
